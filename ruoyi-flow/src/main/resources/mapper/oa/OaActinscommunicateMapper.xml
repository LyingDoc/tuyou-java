<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.flow.oa.mapper.OaActinscommunicateMapper">
	
	<!-- 查询数据
	<select id="findList" resultType="OaActinscommunicate">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="queryTodoCommunicate" resultType="com.ruoyi.flow.oa.entity.OaActinscommunicateEntity">
   select
    *    from oa_actinscommunicate
       where communicate_status=1 and approversuser=#{fapproversuser} and oa_actins_id=#{factinsid} and oa_processins_id=#{fprocessinsid}
    </select>
</mapper>