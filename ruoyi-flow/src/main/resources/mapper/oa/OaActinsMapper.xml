<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.flow.oa.mapper.OaActinsMapper">
	
	<!-- 查询数据
	<select id="findList" resultType="OaActins">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="queryTodoActins" resultType="com.ruoyi.flow.oa.entity.OaActinsEntity">
   select
         *
        from oa_actins  where oa_processins_id = #{processinsid} and (actins_Status=0 or actins_Status=1) LIMIT 1
    </select>
</mapper>